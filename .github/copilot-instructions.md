Keep the coordinator's selected model and reasoning. Use the `bulk-reader` skill
for large-file questions and the `code-writer` skill for reference-based
boilerplate. Delegate paths, not pasted file bodies. Start a fresh worker each
time. Debugging, architecture, and surgical edits stay here, using bounded reads.
